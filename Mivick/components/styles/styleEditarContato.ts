import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#000',
        paddingHorizontal: 12,
    },

    dropdown: {
        borderColor: '#F85200',
        borderWidth: 2,
        borderRadius: 8,
    },
});
