import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#000',
    paddingHorizontal: 12,
  },
  cardText: {
    color: '#FFFFFF',
    fontSize: 14,
    marginVertical: 8,
  },
  cardTextSmall: {
    color: '#FFFFFF',
    fontSize: 13,
    marginBottom: 12,
  },
});
